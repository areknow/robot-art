/**
 * Because firebase does not allow signing in without a full email,
 * the work around is to match the 'Admin' user name string and replace
 * it with the full email address from the users database.
 */

export const ADMIN_EMAIL = 'admin@robotart.com';
export const ADMIN_USER_NAME = 'Admin';
